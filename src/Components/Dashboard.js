import { Card, CardContent, CardHeader } from "@mui/material";
import React from "react";

export const Dashboard = () => {
  return (
    <Card>
      <CardHeader title="Dashboard page"></CardHeader>
      <CardContent>React Admin + Hasura</CardContent>
    </Card>
  );
};
